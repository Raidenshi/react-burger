export enum StatusColor {
  done = 'success',
  canceled = 'error',
  created = 'active',
  pending = 'active',
}

export enum Status {
  done = 'Завершён',
  canceled = 'Отменён',
  created = 'Создан',
  pending = 'Готовится',
}
